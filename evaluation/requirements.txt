# vLLM 0.16.0 stable-compatible stack.
#
# This is a clean vLLM 0.16.0 final-release environment. It is close to the
# original pre-SGLang gart setup, but note that the old gart server was running
# a 0.16.0rc2.dev build rather than the final 0.16.0 release.

# Core ML dependencies
torch==2.9.1
torchaudio==2.9.1
torchvision==0.24.1
transformers==4.57.6
accelerate==1.10.0
tiktoken==0.12.0
vllm==0.16.0
huggingface-hub==0.36.2
tokenizers==0.22.2

# vLLM 0.16-sensitive adjuncts
flashinfer-python==0.6.3
llguidance==1.3.0
outlines_core==0.2.11
xgrammar==0.1.29
triton==3.5.1

# Data processing
pandas==3.0.1
numpy==2.2.6

# Configuration
pyyaml==6.0.3

# Progress bar
tqdm==4.67.3
