# CUDA vLLM backend dependencies.
# Install this on the deployment machine that will run the real backend:
#   pip install -r requirements.txt -r requirements-vllm.txt

torch==2.9.1
transformers==4.57.6
vllm==0.16.0
flashinfer-python==0.6.3
llguidance==1.3.0
outlines_core==0.2.11
xgrammar==0.1.29
triton==3.5.1
